<a href="<%= user_path(current_user) %>">
	<%= gravatar_for current_user %>
</a>

<h1>
	<%= current_user.name %>
</h1>

<span>
	<%= link_to "我发布的信息", current_user %>
</span>

<span>
	<%= pluralize(current_user.microposts.count, "post") %>
</span>