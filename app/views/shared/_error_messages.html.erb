<% if object.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
			数据提交错误 <%= pluralize(object.errors.count, "error") %>
		</div>
		<ul>
			<% object.errors.full_messages.each do |msg| %>
				<li>* <%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>